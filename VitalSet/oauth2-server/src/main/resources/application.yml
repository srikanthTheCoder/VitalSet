server:
  port: 9191
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/oauth?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
    initialization-mode: always
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        show_sql: true
        format_sql: true
logging:
  level:
    org:
      hibernate:
        type: trace